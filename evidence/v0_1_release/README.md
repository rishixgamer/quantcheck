# QuantCheck v0.1 public evidence

The complete public v0.1 evidence tree contains 595 files. It was freshly
reproduced and verified from immutable release `v0.1.0`; the complete tree is
published as the release asset
[`quantcheck-v0.1.0-public-evidence.tar.gz`](https://github.com/rishixgamer/quantcheck/releases/download/v0.1.0/quantcheck-v0.1.0-public-evidence.tar.gz).

Verify the downloaded archive with SHA-256:

```text
da778f05f4f27d1fa314046ddbd8f4616b0460d13b7c06374060d639410f2fcf  quantcheck-v0.1.0-public-evidence.tar.gz
```

This directory intentionally contains only four standalone canonical summaries
for stable repository citations: `aggregate_report.json`, `benchmark_config.json`,
`case_matrix.json`, and `runtime_metadata.json`. The complete tree's
`index.json` is not checked in here because it indexes the other 594 files;
use the verified release archive when the full index and case artifacts are
needed.
