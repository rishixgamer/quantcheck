The current handoff identifies **Milestone 11 — Final evidence and release** as the exact next task and requires the thresholds and configurations to be frozen before final release evidence is generated. fileciteturn3file0

Paste this prompt into Codex from the QuantCheck repository root:

:::writing{variant="document" id="63841"}
# Complete QuantCheck Milestone 11 — Final Evidence and Release

You are working inside the existing **QuantCheck Codex Harness v2** repository.

Milestones 0–10 are complete. The current repository has:

- Four deterministic fault families:
  - Look-Ahead
  - Unit Drift
  - Duplicate Observations
  - Revision Overwrite
- Exact injection, sanitized detection, matching, scoring, controlled repair, and research-impact logic
- A deterministic benchmark runner with public/private artifact isolation
- A strict six-command Typer CLI
- A public-only artifact reader
- A read-only Streamlit dashboard
- A deterministic self-contained HTML summary
- 630 passing tests at the Milestone 10 baseline
- Benchmark specification `0.1.0`
- Development seeds `0-9`
- Validation seeds `100-109`
- Reserved final held-out seeds `1000-1009`

Your task is to complete the repository’s actual **Milestone 11 — Final evidence and release**.

Do not use the older external milestone numbering that describes Milestone 11 as a case runner. In this repository, the case runner, CLI, dashboard, and HTML presentation already exist. `IMPLEMENT.md` is the operational source of truth.

Do not merely produce a plan. Inspect the repository, implement the required release work, run the evidence matrix, verify the results, update the documentation and handoff, and report the exact outcome.

---

## 1. Mandatory reading before editing

Read these files first:

- `AGENTS.md`
- `IMPLEMENT.md`
- `README.md`
- `PROJECT_SCOPE.md`
- `MVP_ACCEPTANCE_CRITERIA.md`
- `CHECKSUMS.md`
- `pyproject.toml`
- `uv.lock`
- `docs/DECISIONS.md`
- `docs/CLI_CONTRACT.md`
- `docs/DASHBOARD_AND_HTML.md`
- All benchmark, artifact, privacy, determinism, testing, adversarial-review, release, reproducibility, and fault-contract documents
- All existing configuration files under `configs/`
- The GitHub Actions workflow
- Existing benchmark schemas, expansion, dispatch, persistence, aggregation, CLI, presentation, and HTML implementation
- Existing test helpers and focused benchmark tests
- All release/checklist documents, regardless of their exact filenames

Search the repository for:

- `Milestone 11`
- `release`
- `held-out`
- `final seed`
- `1000`
- `threshold`
- `freeze`
- `120`
- `evidence`
- `adversarial`
- `reproducibility`
- `acceptance criteria`

Build an internal checklist that maps every authoritative release requirement to:

1. its source document;
2. the implementation or artifact that satisfies it;
3. the command that verifies it;
4. its final status.

Authority order:

1. `AGENTS.md`
2. Current `IMPLEMENT.md`
3. Explicit contracts and release checklists
4. Architecture decisions
5. Schemas and existing implementation behavior
6. Tests
7. Lower-authority planning documents

Do not silently resolve conflicts using assumptions. When a required release detail is genuinely unspecified, make the smallest deterministic decision necessary, record it in `docs/DECISIONS.md`, and test it.

---

## 2. Scope boundary

This milestone is for evidence, hardening, packaging, and release readiness.

Do not add:

- New fault families
- Missing-observation support
- Entity-swap support
- Machine learning
- New data vendors
- A database
- Cloud infrastructure
- Authentication or accounts
- Parallel or distributed benchmark execution
- Automatic detector-only repair
- A new dashboard architecture
- New root CLI commands
- A dashboard CLI command
- A general force-overwrite option
- A general held-out-seed CLI option
- A version flag unless an authoritative contract now explicitly requires it
- Browser automation merely to expand scope
- Live trading, returns, Sharpe ratios, or financial-loss claims not supported by the existing research contracts

Do not redesign completed fault logic, schemas, artifact layouts, scoring semantics, null conventions, privacy boundaries, CLI behavior, dashboard behavior, or HTML behavior unless a demonstrated release-blocking defect requires a minimal correction.

Do not weaken or delete existing tests.

Avoid new dependencies. Add one only when an authoritative release requirement cannot reasonably be met with the standard library or existing dependencies. Any new dependency must be justified, narrowly scoped, locked, documented, and tested.

---

## 3. Release-candidate sequence

Follow this sequence exactly.

### Phase A — Inspect and prepare release plumbing

Before using any final held-out seed:

1. Determine the exact release matrix and clean-control policy from the authoritative harness documents.
2. Determine the exact release artifact and documentation requirements.
3. Identify how the current schemas prohibit final seeds.
4. Implement the narrowest explicit release-only authorization needed to execute the reserved final seeds.
5. Do not expose unrestricted held-out execution through the ordinary CLI.
6. Keep ordinary development and validation behavior unchanged.
7. Ensure only the exact reserved final seeds are accepted by the release path.
8. Add tests proving final seeds remain rejected outside the explicit release workflow.
9. Add tests proving unrecognized or out-of-range “release” seeds remain rejected.
10. Document the release-only behavior and rationale.

The expected core fault matrix is:

```text id="fxth11"
4 fault families
× 3 severity levels
× 10 reserved final seeds
= 120 held-out fault cases
```

Also include the exact clean controls required by the authoritative benchmark/release contract. Do not guess whether controls are per fault, severity, or seed if the repository already specifies the rule.

The release matrix must use the reviewed synthetic fixture and run offline unless a higher-authority release document explicitly requires otherwise.

### Phase B — Validation rehearsal

Before freezing the release candidate, run a complete rehearsal using only development or validation seeds.

The rehearsal must exercise:

- All four fault families
- Low, medium, and high severities
- The same detector configurations intended for release
- The same research configurations intended for release
- Required clean controls
- Public/private artifact writing
- Aggregation
- Public-only aggregate reconstruction
- Dashboard loading
- HTML generation
- Failure and incomplete-case handling
- Resume behavior
- Privacy scans
- Determinism checks

Correct release-plumbing defects before the freeze.

Do not tune detector thresholds, matching rules, severity definitions, false-positive denominators, target fractions, research calculations, or case selection to make rehearsal results look better. Existing scientifically meaningful weaknesses must remain visible.

### Phase C — Freeze the release candidate

After the rehearsal and before executing any held-out seed, freeze and record:

- Package/distribution version
- Benchmark specification version
- Fixture identity and hash
- All four fault specifications and component versions
- Detector configurations
- Detector thresholds
- Severity parameters
- Matching rules
- False-positive denominator rules
- Research configurations
- Held-out seed list
- Fault profiles
- Severity list
- Clean-control policy
- Release benchmark configuration
- Relevant source/configuration hashes
- Lockfile state
- Harness checksum state

Use the release checklist’s existing freeze artifact or document if one exists.

If no exact freeze format exists, create the smallest clear machine-readable or Markdown freeze record necessary. It must contain no secrets, absolute local paths, private values, manifests, or hidden answer-key relationships.

Update `CHECKSUMS.md` after the release candidate is frozen.

Run all required quality gates after the freeze and before the held-out benchmark.

### Phase D — Execute the held-out release benchmark

Run the exact frozen release configuration with reserved final seeds `1000-1009`.

Important scientific rule:

> After observing held-out results, do not change detector thresholds, selection rules, matching rules, denominators, severities, or research configurations to improve the metrics.

Preserve the first held-out run and its evidence.

If a genuine implementation or infrastructure bug invalidates the run:

1. Do not delete or silently replace the first result.
2. Record the failure and why the candidate was invalid.
3. Distinguish a correctness bug from poor detector performance.
4. Make only the minimal correctness repair.
5. rerun all quality gates;
6. create a new release-candidate freeze record;
7. run the held-out matrix again as a clearly identified new candidate.

Do not describe ordinary false positives, missed detections, or weak severity performance as software bugs merely because they lower the score.

Every failed or incomplete case must remain visible in the saved matrix and aggregate report. Do not silently exclude it.

---

## 4. Required final benchmark evidence

Produce and verify the complete saved benchmark artifact tree required by the existing contracts.

At minimum, verify:

- The exact configured case count
- Successful case count
- Failed case count
- Incomplete case count
- Injected-fault count
- Finding count
- True-positive fault count
- False-negative fault count
- True-positive finding count
- False-positive finding count
- Eligible clean denominator
- Overall micro precision
- Overall recall
- Overall F1
- Overall false-positive rate
- Metrics grouped by fault family
- Metrics grouped by severity
- Metrics grouped by final seed
- Research-output change counts
- Exact-replay restoration counts
- Clean-control behavior
- Failure categories, if any

Report exact saved Decimal strings. Do not convert through binary float or round values inconsistently between the aggregate report, HTML, dashboard, README, and technical report.

Analyze, without hiding weak results:

- Which fault family had the highest and lowest precision
- Which fault family had the highest and lowest recall
- How results changed across low, medium, and high severity
- Where false positives occurred
- Where false negatives occurred
- Which cross-detector findings reduced strict primary precision
- Which fault families most often changed the controlled research output
- Whether all successful repaired cases reproduced the clean research result
- Whether any cases failed or remained incomplete
- Known fixture and methodology limitations

Do not claim:

- General production readiness
- Automatic remediation
- Financial loss prevention
- Real trading alpha
- General SEC coverage
- Statement reconstruction
- Universal restatement detection
- Vendor-wide data validation

---

## 5. Public/private and release-artifact isolation

Treat public/private separation as a release-blocking requirement.

Prove that the public release evidence contains no:

- Manifest
- Hidden role relationship
- Original or reference answer-key value
- Pre-corruption value
- Private research value
- Target record ID list
- Target count not approved for public release
- Private diagnostic
- Local absolute path
- User home directory
- Temporary directory
- Cache directory
- Access token
- Credential
- Contact email
- Raw SEC cache
- Symlink escape
- Traversal path
- Reference into `private/`

Create or verify a public-only release copy containing the approved `public/` tree and approved report/documentation artifacts.

Remove or omit the entire private tree from this copy.

Using only the public-only copy:

1. Load it through `public/artifact_index.json`.
2. Reconstruct or validate the aggregate report.
3. Build the immutable presentation model.
4. Render the deterministic HTML summary.
5. Run the Streamlit AppTest.
6. Start the local dashboard health check if permitted.
7. Confirm no benchmark, injector, detector, scorer, repair, or private research logic executes.
8. Scan serialized presentation data and HTML for private fields, secrets, and local paths.

The public-only release must remain useful with the private tree completely absent.

Do not make the dashboard capable of browsing manifests or private values.

---

## 6. Determinism and reproducibility

Verify release reproducibility under the existing contracts.

At minimum test:

- Repeated use of the frozen logical configuration
- A different output root
- Reordered configuration mappings or lists where normalization permits it
- Public-only aggregate reconstruction
- Public-only presentation loading
- HTML rendering to two different destinations
- `PYTHONHASHSEED=1`
- `PYTHONHASHSEED=987654`
- Subprocess execution
- Canonical serialization and reload
- Resume of an identical successful benchmark
- Conflict rejection for differing bytes at an existing immutable destination

Compare the correct classes of output:

- Logical identifiers must remain identical.
- Canonical logical artifacts must remain identical where the contract requires it.
- Runtime timestamps and environment metadata must not be confused with logical identity.
- HTML must remain byte-identical where its contract requires it.
- Do not claim that runtime metadata is byte-identical when the schema intentionally records runtime facts.

Record:

- Release benchmark ID
- Aggregate report ID
- Public index identity or hash
- Final HTML SHA-256
- Release configuration SHA-256
- Freeze-record SHA-256
- Any approved public archive SHA-256
- Package artifact hashes when applicable

---

## 7. Documentation and evidence package

Use existing document paths when they already exist. Do not create duplicate documents with overlapping purposes.

Complete or update the release documentation required by the harness, including the equivalent of:

- Installation guide
- Five-minute quick start
- Exact smoke command
- Exact final release benchmark reproduction command
- Dashboard launch command
- HTML summary command
- Architecture overview
- Point-in-time methodology
- Fault catalog
- Scoring methodology
- Research-impact methodology
- Public/private artifact model
- Reproducibility guide
- Threat model or adversarial review
- Final benchmark-results report
- Failure analysis
- Known limitations
- Contributor guidance
- Changelog
- Release checklist
- Release notes
- Résumé-ready project description

The benchmark-results or technical report must be generated from the final saved artifacts, not manually invented numbers.

Every public metric appearing in:

- README
- technical report
- results table
- HTML
- dashboard
- release notes

must match the saved aggregate report exactly.

Include honest limitations, especially:

- Synthetic reviewed fixture
- Four fault families only
- No missing-observation or entity-swap support
- Controlled research comparisons rather than a trading backtest
- Manifest-assisted exact replay rather than automatic remediation
- Unit Drift’s immediate-neighbor limitations
- Duplicate v0.1’s exact-copy scope
- Revision Overwrite’s narrow source-supported history rules
- SEC adapter’s one-CIK synchronous scope
- No live SEC attestation in this environment
- No statement reconstruction
- No database, cloud, parallel scheduler, or hosted platform
- Local read-only Streamlit
- No manifest browsing in the public presentation
- Any final held-out misses or false positives

Do not manufacture a demonstration video, hosted site, public GitHub URL, DOI, release URL, or CI badge that does not exist.

You may provide a precise script/storyboard for a three-minute demonstration if the release checklist requests it, but do not claim that a video was recorded unless it actually was.

---

## 8. Packaging and local release verification

Inspect the authoritative release checklist and complete every locally possible packaging requirement.

At minimum, where compatible with the existing project:

1. Confirm the package version is exactly the intended `0.1.0` release version.
2. Validate `pyproject.toml` metadata.
3. Validate the license and changelog requirements.
4. Build the wheel and source distribution using the existing `uv`/Hatchling toolchain.
5. Inspect package contents.
6. Confirm no benchmark private artifacts, raw SEC caches, secrets, temporary files, or local paths are packaged.
7. Install the wheel into a clean temporary environment.
8. Run:
   - CLI help
   - a small offline smoke benchmark
   - public artifact loading
   - HTML rendering
9. Record package hashes.
10. Ensure ordinary `import quantcheck` does not import Streamlit.

Do not publish to PyPI or another registry unless explicitly authorized.

Do not create fake Git history or fake tags.

If `.git` metadata is present:

- inspect the actual diff;
- verify tracked release files;
- confirm no secrets or large caches are staged;
- prepare the repository for a `v0.1.0` tag;
- do not push or publish unless explicitly authorized.

If `.git` metadata is absent:

- do not initialize a replacement repository merely to claim success;
- document that commit/tag verification could not be performed;
- use direct file inspection and checksums;
- describe the result as locally release-ready or a release candidate where appropriate.

If GitHub Actions cannot be executed in this environment:

- validate the workflow syntax and commands locally as far as possible;
- do not claim remote CI passed;
- state that remote CI remains an external release gate.

---

## 9. Adversarial release review

Run the release/adversarial checklist against the finished candidate.

At minimum verify that:

- Detectors cannot receive manifests.
- Detectors cannot receive hidden original/reference values.
- Audit inputs contain no seed, severity, target count, expected finding count, rank, or answer-key role.
- Fault count cannot be inferred from a leaked field.
- Duplicate findings cannot inflate recall.
- Cross-detector findings remain visible under strict primary precision.
- Empty/no-fault cases use the documented null and precision conventions.
- Failed and incomplete cases remain visible.
- Aggregate metrics can be reconstructed from public case artifacts.
- Reordering does not change target selection or identities.
- Fixture IDs are not memorized by detectors.
- Clean unusual observations remain hard negatives.
- Public paths cannot cross case boundaries or enter private storage.
- Symlink escapes and traversal remain rejected.
- Artifact conflicts never silently overwrite.
- Interrupted writes do not appear successful.
- Presentation code does not execute benchmark logic.
- Release reports do not selectively omit weak fault/severity groups.

Add focused tests for any release-only behavior not already covered.

---

## 10. Required verification commands

Use the repository’s documented `uv` executable and writable cache strategy when the environment requires it. Treat sandbox/cache/DNS/port restrictions as environmental facts, not reasons to modify project behavior.

Run all authoritative harness commands. At minimum, run the equivalent of:

```bash id="uqqwqp"
uv sync --frozen --all-groups
uv run ruff check .
uv run ruff format --check .
uv run mypy src tests
uv run pytest -q
uv lock --check
```

Also run:

- Extended MyPy for dashboard and scripts if not included by the standard command
- All reviewed fixture `--check` commands
- Golden-vector checks
- Complete checksum verification
- CLI focused suite
- Benchmark focused suite
- Look-Ahead suite
- Unit Drift suite
- Duplicate suite
- Revision Overwrite suite
- SEC adapter suite
- Schema/serialization/audit-boundary suite
- Presentation suite
- Release-only suite
- Explicit subprocess/hash-seed checks
- Smoke benchmark
- Validation rehearsal matrix
- Final held-out release matrix
- Public-only aggregate reconstruction
- Public-only HTML rendering
- Streamlit AppTest
- Local package build and clean-install smoke
- Privacy and local-path scans

Do not report a command as passing unless it exited successfully.

Record environmental retries separately from project failures.

Do not hard-code “630 passed” into the final report. Report the actual final count.

---

## 11. Completion criteria

Milestone 11 passes only if:

- The release configuration and thresholds were frozen before held-out execution.
- The release-only final-seed path is explicit, narrow, and tested.
- The required held-out matrix ran from the frozen configuration.
- At least the required 120 fault cases are represented, plus required controls.
- Failed and incomplete cases are honestly preserved.
- The aggregate is reconstructable from public artifacts alone.
- Public presentation works with the private tree absent.
- Final metrics are derived from saved artifacts.
- README, report, dashboard, HTML, and release notes agree.
- Privacy scans pass.
- Determinism requirements pass.
- Package build and clean-install smoke pass.
- Existing completed behavior remains green.
- Every locally possible release-checklist item is complete.
- Unavailable external gates such as remote CI, Git tag, hosting, or publication are explicitly identified rather than fabricated.
- `IMPLEMENT.md` and `CHECKSUMS.md` are current.

Do not mark Milestone 11 complete merely because the benchmark ran.

---

## 12. Update the handoff

Update `IMPLEMENT.md` concisely and factually.

If all locally controllable gates pass, set the current phase to the equivalent of:

```text id="g9j8y9"
Milestone 11 complete — Final Evidence and Release
```

Record:

- Exact milestone completed
- Final release-candidate or release status
- Freeze mechanism
- Release configuration
- Exact configured/successful/failed/incomplete case counts
- Overall metrics
- Important grouped results
- Research-change and repair-restoration totals
- Benchmark/report/index/configuration/HTML/package hashes
- Public-only verification
- Package verification
- Tests added
- Final test count
- Commands run and outcomes
- Files changed
- Dependencies added or changed
- Decisions added or revised
- Known limitations
- External gates not verifiable locally
- Exact next task

The next task should not be another unfinished MVP milestone. It should be one of:

- complete external publication gates;
- perform the human browser/video review;
- run remote CI and create the tagged release;
- begin an explicitly selected post-MVP milestone.

If a required local gate fails, do not mark the milestone complete. Record the exact blocker and the smallest next task.

Update `CHECKSUMS.md` last and run the complete checksum verifier again.

---

## 13. Final Codex response format

Return a detailed but factual final report with these sections:

```text id="mmf7w7"
## Implementation status
Pass / Partial / Fail

## Release freeze
- What was frozen
- Configuration path
- Candidate identity and hashes
- Confirmation that held-out seeds were not used before freeze

## Final benchmark
- Configuration
- Case counts
- Overall metrics
- Grouped highlights
- Failures/incomplete cases
- Research-impact and replay results

## Evidence package
- Reports and documentation created
- Dashboard/HTML verification
- Public-only verification

## Privacy and adversarial review
- Isolation tests
- Scan outcomes
- Remaining risks

## Packaging and release readiness
- Version
- Build artifacts and hashes
- Clean-install result
- Git/CI/tag/publication status

## Files changed
- File-by-file purpose

## Tests added
- Test file and purpose
- Final counts

## Verification results
- Exact commands
- Exit codes
- Focused suite counts
- Determinism evidence

## Decisions and dependencies
- ADRs
- Dependency changes or confirmation of none

## Known limitations
- Honest scientific, implementation, and release limitations

## Handoff
- Whether Milestone 11 is complete
- Exact next task
```

Include exact values and hashes from the generated artifacts.

Do not claim success without evidence. Do not hide poor held-out results. Do not optimize against the final seeds. The objective is a trustworthy, reproducible release—not an artificially impressive score.
:::

This version makes the most important release safeguard explicit: Codex must complete a validation rehearsal and freeze the release candidate **before** it is allowed to run seeds `1000-1009`.